---
phase: 04-multi-org-local
plan: 01
type: execute
---

<objective>
Add GITHUB_ORGS env var support so the scanner fetches repos from multiple GitHub organizations alongside the user's personal repos. Verify local-only project discovery works through the UI scan path.

Purpose: Closes the gap where CLI supports --org but UI doesn't, and extends to support multiple orgs via a single env var.
Output: Scanner and API route support GITHUB_ORGS=org1,org2 for multi-org scanning in both CLI and UI.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@scanner/index.ts
@scanner/github.ts
@src/app/api/scan/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GITHUB_ORGS support to scanner</name>
  <files>scanner/index.ts, scanner/github.ts</files>
  <action>
1. In scanner/index.ts ScanOptions interface, add `orgs?: string[]` alongside existing `org?: string`.
2. In runScan(), after fetching user repos, iterate over each org in `orgs` array and call fetchAllRepos for each org. Merge the results (dedup by repo name, org repos take precedence if name conflicts — prefix with org name like `org/repo` to avoid collisions).
3. In the main() function, parse GITHUB_ORGS env var (comma-separated string → string array). Pass as `orgs` to runScan. Keep existing --org flag working — if --org is provided, add it to the orgs array.
4. In scanner/github.ts fetchAllRepos(), no changes needed — it already accepts an `org` parameter. The loop in runScan will call it multiple times.
5. Update progress events to reflect total across all sources (user + all orgs).
  </action>
  <verify>
Run `GITHUB_ORGS=test-org npm run scan` and verify it attempts to fetch repos for both the user and the org. Check that --org flag still works standalone.
  </verify>
  <done>
- ScanOptions has `orgs?: string[]` field
- GITHUB_ORGS env var parsed as comma-separated list
- Scanner fetches user repos + all org repos in one scan
- --org flag still works (added to orgs array)
- No duplicate repos in manifest
  </done>
</task>

<task type="auto">
  <name>Task 2: Pass GITHUB_ORGS through API route and verify local-only discovery</name>
  <files>src/app/api/scan/route.ts</files>
  <action>
1. In the scan API route, read GITHUB_ORGS env var (same parsing as CLI: split by comma, trim whitespace).
2. Pass `orgs` array to runScan() call alongside existing token, username, localDir.
3. Verify that local-only discovery already works in the UI scan path — the existing code passes localDir to runScan, and runScan already discovers local-only projects when localDir is set. Confirm this path is complete by reading the code flow. If there's a gap, fix it.
4. Do NOT add any new UI components — the orgs are configured via env var only.
  </action>
  <verify>
Read the scan route code and confirm GITHUB_ORGS is parsed and passed. Trace the code path for local-only discovery through the API route to confirm it works end-to-end.
  </verify>
  <done>
- API route reads GITHUB_ORGS and passes orgs array to runScan
- Local-only discovery path confirmed working through UI scan
- No new UI needed — env var configuration only
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx vitest run` passes (no regressions)
- [ ] `npm run build` succeeds
- [ ] Scanner handles GITHUB_ORGS="" (empty) gracefully
- [ ] Scanner handles GITHUB_ORGS with single org
- [ ] Scanner handles GITHUB_ORGS with multiple orgs
- [ ] --org CLI flag still works independently
</verification>

<success_criteria>
- GITHUB_ORGS env var works in both CLI and UI scan paths
- Multiple orgs are scanned alongside user repos
- No duplicate repos in manifest output
- Local-only discovery confirmed working in UI
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-org-local/04-01-SUMMARY.md`
</output>
